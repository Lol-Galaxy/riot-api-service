PORT=8080
# Origines autorisées (séparées par des virgules)
# La clé Riot est transmise par chaque app via header X-Riot-Token — pas stockée ici
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000
